import { ToolConfig } from "../types";
import { Sparkles } from "lucide-react";

export const config: ToolConfig = {
  id: "prompt-painter-react",
  name: "Prompt Painter React",
  description: "Create and visualize HTML designs with live preview and Tailwind CSS support.",
  icon: "Sparkles",
  category: "Design",
  featured: true,
  tags: ["html", "css", "tailwind", "design", "preview", "editor"]
};